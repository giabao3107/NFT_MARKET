{
  "name": "nft-marketplace",
  "version": "1.0.0",
  "description": "A decentralized NFT marketplace built with React, Solidity, and MetaMask",
  "main": "index.js",
  "scripts": {
    "start": "react-scripts start",
    "prebuild": "npm run compile && npm run copy-contracts",
    "build": "react-scripts build",
    "copy-contracts": "node -e \"const fs = require('fs'); const path = require('path'); if (!fs.existsSync('src/contracts')) fs.mkdirSync('src/contracts', {recursive: true}); fs.copyFileSync('artifacts/contracts/NFT.sol/NFT.json', 'src/contracts/NFT.json'); fs.copyFileSync('artifacts/contracts/NFTMarketplace.sol/NFTMarketplace.json', 'src/contracts/NFTMarketplace.json'); console.log('✅ Contract artifacts copied');\"",
    "test": "react-scripts test",
    "eject": "react-scripts eject",
    "compile": "npx hardhat compile",
    "deploy": "npx hardhat run scripts/deploy.js --network localhost",
    "node": "npx hardhat node",
    "setup": "npx hardhat run scripts/setup-local-accounts.js --network localhost",
    "dev": "npm run node & npm run setup && npm start"
  },
  "dependencies": {
    "axios": "^1.3.4",
    "buffer": "^6.0.3",
    "ethers": "^5.7.2",
    "ipfs-http-client": "^60.0.1",
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "react-icons": "^4.7.1",
    "react-router-dom": "^6.8.1",
    "react-scripts": "5.0.1",
    "react-toastify": "^9.1.1",
    "@nomiclabs/hardhat-ethers": "^2.2.2",
    "@nomiclabs/hardhat-waffle": "^2.0.4",
    "@openzeppelin/contracts": "^4.8.1",
    "hardhat": "^2.12.7"
  },
  "devDependencies": {
    "chai": "^4.3.7",
    "ethereum-waffle": "^4.0.10"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
